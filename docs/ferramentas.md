# Ferramentas

FGTS: Guia de Arrecadação expõe 1 ferramenta (todas somente leitura).

### 1. `fgts_guia_consultar`
**Input**: `pagina` (opcional), `periodo` (opcional), `representado` (opcional), `login_cpf`, `login_senha`, `pkcs12_cert` (opcional), `pkcs12_pass` (opcional)

FGTS: Guia de Arrecadação, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar FGTS: Guia de Arrecadação
```
